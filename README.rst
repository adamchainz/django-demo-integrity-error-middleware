django-demo-integrity-error-middleware
======================================

.. code-block:: console

    python -m venv venv
    source venv/bin/activate
    pip install django
    python manage.py migrate
    python manage.py runserver

Then load http://localhost:8000 .
